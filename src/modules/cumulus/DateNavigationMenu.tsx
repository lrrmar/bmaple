import React from 'react';
import './DateNavigationMenu.css';

const DateNavigationMenu = (props: any) => {
  return <div className="DateNavigationMenu">{props.children}</div>;
};

export default DateNavigationMenu;
